{% set name = "mapcat" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mapcat-{{ version }}.tar.gz
  sha256: 244e54b4c1dfc2c82bc1b6340a0025d16839f1ac0ef75b45977d80f560d71416

build:
  noarch: python
  entry_points:
    - actingest = mapcat.toolkit.act:main
    - mapcatmigrate = mapcat.helper:migrate
    - mapcatreset = mapcat.toolkit.reset:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 1

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - sqlmodel
    - sqlalchemy
    - aiosqlite
    - alembic
    - pydantic-settings
    - pixell
    - h5py
    - astropydantic

test:
  imports:
    - mapcat
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check
    - actingest --help
    - mapcatmigrate --help
    - mapcatreset --help

about:
  home: https://pypi.org/project/mapcat/
  summary: The Depth-1 and Atomic MAP CATalog for the Simons Observatory.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - iparask
